// Define a função 'soma' que recebe dois parâmetros: 'a' e 'b'.
function soma(a, b) {
    // Exibe no console o resultado da soma de 'a' e 'b'.
    console.log(a + b)
}

// Exporta a função 'soma' como a exportação padrão do módulo.
// Isso permite que ela seja importada por outros módulos usando 'import soma from ...'.
export default soma;
