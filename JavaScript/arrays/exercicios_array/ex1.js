// Objetivo: Criar um array e exibir todos os seus elementos usando um loop for.




let numeros = [10, 20, 30, 40, 50];

for (let i = 0; i < numeros.length; i++) {
  console.log(numeros[i]);  // Exibe cada número do array
}
